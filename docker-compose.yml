services:
  md-converter:
    build: .
    ports:
      - "3200:3200"
    volumes:
      - ./correos:/app/correos
      - ./md_output:/app/md_output
    environment:
      - WATCH_DIR=/app/correos
    restart: unless-stopped
