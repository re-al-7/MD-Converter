services:
  md-converter:
    build: .
    ports:
      - "3200:3200"
    volumes:
      - ./correos:/app/correos
      - ./md_output:/app/md_output
    environment:
      - WATCH_DIR=/app/correos
      - OUTPUT_DIR=/app/md_output
    restart: unless-stopped
